# Risk Register — Cowork Starter Kit

**Scope note:** this register was seeded at v2.19.2 ("Distribution & Trust") with **open items only** — the accepted-risk decisions currently carried forward, unresolved, at the time of seeding. A full historical backfill across every closed risk in `docs/retro.md` is a separate, larger future effort and is intentionally **not** attempted here (see `docs/retro.md` for the complete historical record in the meantime). Every row below cites the cycle/doc that accepted it; no row asserts a risk is closed without that citation, and no row's status is guessed — an unclear status is recorded as `STATUS-UNCLEAR — needs owner confirmation`, never silently resolved either way.

| ID | Description | Accepted under what condition | Status | Source |
|----|---|---|---|---|
| `v2.20-CARRY-1` | The v2.18.0 deterministic pattern-scan is scoped to `## Example` sections only (an implementation-time deviation from a whole-file scan, accepted to avoid false-positiving on ordinary English across most of the existing skill pool). This is safe only as long as all content entering the pool is maintainer-authored/curated. Once v2.20 community intake accepts genuinely untrusted content, the `## Example`-only scan becomes structurally insufficient — it does not cover injection planted outside an `## Example` block. | Accepted at v2.18.0 Phase 6 (`(b) ACCEPT with two binding conditions`) specifically because v2.18.0 ships to curated-only traffic. The v2.20 semantic LLM-judge (already deferred, per `docs/roadmap.md`) **MUST** extend scan coverage beyond `## Example` before community intake opens. | **OPEN** — binding on v2.20's own `/spec`; untouched by this cycle (out of scope, confirmed at v2.19.2 Phase 0). | `docs/retro.md:113` (v2.18.0 entry, `A2 / v2.20-CARRY-1`) |
| `SF-2` | v3.0's Class-2-before-Class-1 execution ordering (does a higher-ceremony self-integrity check run before an ordinary engine-file write, when both could apply to the same target) was only PARTIALLY addressed at v2.19.0 — `self-upgrade` ships dormant, so no Class-1 executable path exists yet to exercise the ordering against. | Accepted as INFO (non-blocking) at v2.19.0 Phase 6 specifically because the dormant skill has zero live invocation targets at v2.19 — nothing fires yet, so nothing to verify yet. Matures into a binding check the first cycle that gives `self-upgrade` a real forward-walk target (v3.0 or later). | **OPEN** — INFO carry, matures at v3.0. | `docs/security-audit-v2.19.0.md:34,53` |
| `SF-3` | Installed-base `sha256` drift re-verification (re-confirming an already-installed skill's on-disk bytes still match the curated registry's recorded hash, beyond the point of initial install/backfill) has no standing re-check mechanism yet. | Accepted as INFO (non-blocking) at v2.19.0 Phase 6; `pull-updates` computes fresh bytes at time of use (AC-PULL-6), which covers the pull-decision path, but there is no independent periodic drift audit outside that flow. Named as an ADR-073 maturation item. | **OPEN** — INFO carry, no committed target rung yet. | `docs/security-audit-v2.19.0.md:35,53` |
| `SF-4` | The v2.19 migration-provenance log (`context/.kit-migrations/`) is append-only and deny-listed (tamper-*resistant*), but has no cryptographic hash-chaining (tamper-*evident* — the ability to prove after the fact that no entry was silently altered or removed). | Accepted as PARTIAL → INFO carry at v2.19.0 Phase 6: append-only + deny-listed was judged sufficient containment for a dormant capability with no real entries yet. Hash-chaining is named as a v3.x hub-era item (the hub view is the first consumer that reads this log across multiple spaces and would most benefit from tamper-evidence, not just tamper-resistance). | **OPEN** — INFO carry, no committed target rung yet (loosely associated with v3.x, not binding on it). | `docs/security-audit-v2.19.0.md:36,53` |
| `v2.19.5-CODEOWNERS-1` | No enforced review gate exists over PRs that touch `cowork.lock.json` (or any other CODEOWNERS-listed supply-chain file), and v2.19.5 restores a third-party content-ingestion path (`sync-agency.yml`) that had been dormant (frozen pin) since 2026-07-01. Four enforcement facts, verified live against this repo (ADR-028 v2.19.5 amendment §(d)): (1) no step in `sync-agency.yml` gates on a content-scan `requires_review` hit — it is labeled and warned about, never blocked; (2) the only post-PR-creation `exit 1` (the SPDX-drift step) is structurally unreachable, since the accumulator hardcodes `--arg spdx "MIT"` against 110/110 currently-`"MIT"` entries; (3) **no CI runs on a sync PR at all** — verified against the two real historical sync PRs, `#27`/`#31` (`statusCheckRollup=0` on both, `required_status_checks` not enabled) — so every `quality.yml` job, including the new `sync-verify-ratchet`, is a post-merge detector on this path, never a gate; (4) branch protection requires **zero** approvals and **zero** passing checks (`approvals=0`, `codeowners=false`, `rulesets=0`). **Fifth fact, now partially closed by this cycle:** all six supply-chain `.github/CODEOWNERS` rows named `@msitarzewski` — the **upstream** repo's owner, not a collaborator here — and were therefore **inert**, not merely unenforced, compounding facts 1-4 with a second, independent absence. v2.19.5 re-points those six rows to `@jmlozano1990` (the sole collaborator), which makes the rows *capable* of enforcing something for the first time — but branch protection still does not require CODEOWNERS review (fact 4), so the review gate itself remains OFF. | Accepted at v2.19.5 Phase 2/3 (`@security`'s Phase-0.D CRITICAL disposition, option (b) of `AC-SYNC-CODEOWNERS-1`: correct the false claim rather than mandate an unperformable action — enabling the actual gate needs owner-level repo Settings access no pipeline agent holds). The owner explicitly accepted this exposure at the Phase 3 gate (2026-08-04), with the concrete numbers in front of them (43 net-new + 15 modified files queued for the next successful sync). Discharge routed to `docs/owner-tasks.md` OT-7, a two-step remedy (re-point CODEOWNERS rows, done this cycle; then enable branch protection, still open). | **OPEN** — binding until OT-7 step 2 lands. Close this row (or move it to "Closed this cycle") only after `require_code_owner_reviews` is confirmed enabled via `gh api repos/:owner/:repo/branches/main/protection`. | `docs/architecture.md` ADR-028 v2.19.5 amendment §(d); `docs/owner-tasks.md` OT-7; `docs/security-review-v2.19.5.md` S3/S5 |
| `AC-PUB-10` | Pre-`v2.18.0` release/CHANGELOG mismatch, 3 items (not 5 — a Round-1 miscount corrected at v2.19.6 Phase 1; `v1.3.1.1`/`v1.3.2.1` both have real entries and were wrongly also listed). `v1.0.0` (`CHANGELOG.md:1038`) and `v1.1.1` (`CHANGELOG.md:991`) have dated CHANGELOG sections but no origin tag. `v2.0.1` has an origin tag but no CHANGELOG section. All three are below the standing gate's `v2.18.0` floor, so `verify-release-surface.sh` does not (and by design should not, absent a floor change) surface them. | Recorded, not resolved, at v2.19.6 Phase 1 (`docs/spec.md` `AC-PUB-10`) — documentation only, no target cycle. Extending the gate below the floor is a named future option once the anchor-form predicate is generalized (`docs/architecture.md` ADR-077 §Maturation Path option (a), ADR-078 §Maturation Path option (b)). | **OPEN** — documentation-only carry, no target rung. | `docs/spec.md` v2.19.6 `AC-PUB-10`; `docs/architecture.md` ADR-077/ADR-078 §Maturation Path |
| `v2.19.7-LEDGER-FP` | **`scripts/verify-lock-removals.sh` (the `vendored-removal-ledger` CI job, ADR-080) is expected to run RED on the v2.19.7 PR itself, on its own introducing commit.** The job asserts `blocked_files` never silently shrinks (`AC-B5-8`) by a literal path-string diff between the previous and new `.cowork-allowlist.json` revisions. v2.19.7 repairs the pre-existing `nexus-strategy.md` `blocked_files` entry from a bare basename to its full `marketing/nexus-strategy.md` path (ADR-080 §Context (1) — the bare-basename form never actually matched `sync-agency.yml:228`'s full-path comparison, so it never fired via `blocked_files`; the file was protected only by its `blocked_patterns` entry). That path-string change is, byte-for-byte, indistinguishable from a removal to the ledger's literal-string check — it is a **false positive against `AC-B5-8`'s intent** (no protection is lost: the file remains blocked by both the new full-path `blocked_files` entry and the unchanged basename `blocked_patterns` entry) but a **true positive against the literal check as built** (the exact string `"nexus-strategy.md"` is genuinely absent from the new `blocked_files[].path` set). Verified live by @qa (Phase 5) and independently by @dev (Phase 4) — both reproduced the RED and confirmed the false-positive read is correct. | Accepted at v2.19.7 Phase 5 (`@qa`), on `@security`'s S11 grounds (already recorded in ADR-080 §Maturation Path): do not add a REPAIRED classification dimension to a removal-detection control introduced in this same cycle — doing so raises the odds the new control itself is wrong, which is the exact risk this cycle exists to reduce. No new control logic was written; this row is the accepted-risk record instead. A REPAIRED classification (symmetric with the existing MOVED classification, `AC-B5-9`) is named as a follow-up candidate in ADR-080 §Maturation Path, to be picked up alongside that section's existing S11 trigger. | **SELF-RESOLVING AT MERGE** — this row documents a one-time, already-understood RED on this PR's own `vendored-removal-ledger` run; it is not a forward-carried risk. Once this PR merges, `main`'s `blocked_files` already contains the repaired full path, so no future PR's base→head diff reproduces this specific false positive (a *different* future rename would need its own accepted-risk record, not a re-use of this one). Retained here as the record, not moved to "Closed this cycle," because the RED itself has not yet been observed by CI as of this row being written (Phase 4/5, pre-merge). | `docs/architecture.md` ADR-080 §Amendment/§Maturation Path; `.cowork-allowlist.json` diff (this PR); QA-1 (Phase 5 finding) |
| `CF-v2.19.6-A` | **The ADR-076 D1 pre-create design is incompatible with `softprops/action-gh-release`'s update path, and defeats itself on real execution.** The action takes its **create** path when no Release exists (how `v2.19.0`–`v2.19.3` all succeeded, 2 assets each) and its **update** path when one does. Pre-creating the Release — which is the entire point of ADR-076 D1 — is precisely what forces the update path, which returns **403 `Resource not accessible by integration`**. Proven by direct comparison of two production run logs, not inferred: run `30253778595` (`v2.19.3`) logged `👩‍🏭 Creating new GitHub release for tag v2.19.3...` → success, 2 assets; run `31178636769` (`v2.19.4`) logged `Found release v2.19.4 (with id=366735757)` → `updateRelease` → 403. **Ruled out by checking rather than assuming:** workflow `permissions: contents: write` is byte-identical at `5fee6f9` and `main`, and the four earlier successful runs prove the token can write — this is an **operation-level** refusal, not an under-declared permission. **Explicitly NOT proven:** that repo `default_workflow_permissions` is `read`. Recorded as a lead for v2.19.7 to test, never as the answer — do not cite it as the cause. **Public state left behind:** `v2.19.4` has tag + Release + correct body + **0 assets** (strictly better than the pre-cycle "no tag, no Release"; precedented by `v2.12.0`, which has sat at 0 assets since a prior failed run). `v2.19.5`/`v2.19.6` unpublished; `/releases/latest` still resolves to `v2.19.3`. | Accepted by explicit owner decision 2026-08-07 (option 1 of 3: "diagnose properly, route the fix to v2.19.7, leave `v2.19.4` as-is"), taken with the proven run-log comparison in front of them. Scope A halted at publish 1 of 3 per its own pre-declared stop condition — publishes 2 and 3 were never attempted, and the tag was **not** deleted (the script's own printed step-2 remedy, "delete and re-push the tag", is the WRONG remedy here: `AC-PUB-13`/@security's S7 said check `gh run list` before any deletion and never delete a tag with a live Release attached on a slow poll alone — guidance written for exactly this moment, which held and prevented destroying a live public Release for the wrong reason). **Recommended v2.19.7 fix: have `publish-release.sh` upload the assets itself** — it already holds a token that can update and already builds the archives' provenance, and doing so **removes the workflow dependency entirely**, retiring the ADR-076 D3 question rather than continuing to depend on it. Alternatives rejected: flipping the repo default workflow permission is an owner Settings action that broadens permissions repo-wide to fix one job; changing the action or its invocation keeps a two-step design whose two steps disagree about who owns the Release. | **OPEN** — blocking `AC-PUB-3` and `AC-PUB-15`; `AC-PUB-2` partially met (tag + Release + body, no assets); `AC-PUB-1` State C not reached. Binding on v2.19.7's `/spec`. Close only after a real `publish-release.sh` run produces a Release with 2 assets attached. | `.claude/projects/claude-cowork-config/scratchpad.md` (Scope A halt, 2026-08-07T12:45:00Z); `docs/architecture.md` ADR-076 D1/D3; run logs `30253778595` / `31178636769` |

## Closed this cycle (for context — not carried as open rows above)

- `CF-v2.19-A` (self-apply Quality-criteria prose staleness) and `CF-v2.19-B` (semver-compare.sh malformed-input exit-code contract) were both labeled "next-patch non-blocking" INFO carries at v2.19.0 Phase 6 (`docs/security-audit-v2.19.0.md:51-52`) and were folded into and closed by this cycle (v2.19.2, AC-DIST-8/AC-DIST-9). They are not open items and do not appear as rows above.
- `CF-v2.19.5-F` — closed at v2.19.6 Phase 1/4 as **MISDIAGNOSED, not fixed** (`AC-PUB-8`). The
  original row claimed all five pre-existing releases (`v2.18.0`–`v2.19.3`) "fail permanently
  with no remedy path" because the post-condition rejected any body without the dotted version
  string. Re-diagnosed live 2026-08-07 by reading all five bodies through `gh release view`:
  every one of them **does** name its version, in the house anchor form
  (`CHANGELOG.md#<dots-stripped>---`) GitHub's slugifier produces from this repo's own
  `## [x.y.z] - date` headers — the bodies were never broken. `v2.19.1`'s single dotted-form hit
  was an accidental pass, traced to an unrelated editorial-provenance sentence added at
  v2.19.2, not evidence the old predicate worked. The other four were never broken either. The
  prescribed fix — widen the `gh release edit` repair branch to fire on "missing version
  string" — would have overwritten four curated, irreplaceable editorial bodies (as little as
  652 bytes each) with raw multi-kilobyte CHANGELOG excerpts, destroying content to repair
  nothing. **Actual fix:** the predicate itself was too narrow (dotted-only); widened to accept
  the anchor form too, in one shared definition (`scripts/release-predicate.sh`) — see
  `docs/architecture.md` ADR-077 §D1/§D2.

## Maintenance

Add a row here when a future Phase 2/Phase 6 security review accepts a risk under an explicit condition rather than fully resolving it. Remove (or move to a "Closed" note, as above) a row once the accepting cycle's condition is fully discharged — cite the closing cycle/doc when doing so.
