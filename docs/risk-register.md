# Risk Register — Cowork Starter Kit

**Scope note:** this register was seeded at v2.19.2 ("Distribution & Trust") with **open items only** — the accepted-risk decisions currently carried forward, unresolved, at the time of seeding. A full historical backfill across every closed risk in `docs/retro.md` is a separate, larger future effort and is intentionally **not** attempted here (see `docs/retro.md` for the complete historical record in the meantime). Every row below cites the cycle/doc that accepted it; no row asserts a risk is closed without that citation, and no row's status is guessed — an unclear status is recorded as `STATUS-UNCLEAR — needs owner confirmation`, never silently resolved either way.

| ID | Description | Accepted under what condition | Status | Source |
|----|---|---|---|---|
| `v2.20-CARRY-1` | The v2.18.0 deterministic pattern-scan is scoped to `## Example` sections only (an implementation-time deviation from a whole-file scan, accepted to avoid false-positiving on ordinary English across most of the existing skill pool). This is safe only as long as all content entering the pool is maintainer-authored/curated. Once v2.20 community intake accepts genuinely untrusted content, the `## Example`-only scan becomes structurally insufficient — it does not cover injection planted outside an `## Example` block. | Accepted at v2.18.0 Phase 6 (`(b) ACCEPT with two binding conditions`) specifically because v2.18.0 ships to curated-only traffic. The v2.20 semantic LLM-judge (already deferred, per `docs/roadmap.md`) **MUST** extend scan coverage beyond `## Example` before community intake opens. | **OPEN** — binding on v2.20's own `/spec`; untouched by this cycle (out of scope, confirmed at v2.19.2 Phase 0). | `docs/retro.md:113` (v2.18.0 entry, `A2 / v2.20-CARRY-1`) |
| `SF-2` | v3.0's Class-2-before-Class-1 execution ordering (does a higher-ceremony self-integrity check run before an ordinary engine-file write, when both could apply to the same target) was only PARTIALLY addressed at v2.19.0 — `self-upgrade` ships dormant, so no Class-1 executable path exists yet to exercise the ordering against. | Accepted as INFO (non-blocking) at v2.19.0 Phase 6 specifically because the dormant skill has zero live invocation targets at v2.19 — nothing fires yet, so nothing to verify yet. Matures into a binding check the first cycle that gives `self-upgrade` a real forward-walk target (v3.0 or later). | **OPEN** — INFO carry, matures at v3.0. | `docs/internal/security/security-audit-v2.19.0.md:34,53` |
| `SF-3` | Installed-base `sha256` drift re-verification (re-confirming an already-installed skill's on-disk bytes still match the curated registry's recorded hash, beyond the point of initial install/backfill) has no standing re-check mechanism yet. | Accepted as INFO (non-blocking) at v2.19.0 Phase 6; `pull-updates` computes fresh bytes at time of use (AC-PULL-6), which covers the pull-decision path, but there is no independent periodic drift audit outside that flow. Named as an ADR-073 maturation item. | **OPEN** — INFO carry, no committed target rung yet. | `docs/internal/security/security-audit-v2.19.0.md:35,53` |
| `SF-4` | The v2.19 migration-provenance log (`context/.kit-migrations/`) is append-only and deny-listed (tamper-*resistant*), but has no cryptographic hash-chaining (tamper-*evident* — the ability to prove after the fact that no entry was silently altered or removed). | Accepted as PARTIAL → INFO carry at v2.19.0 Phase 6: append-only + deny-listed was judged sufficient containment for a dormant capability with no real entries yet. Hash-chaining is named as a v3.x hub-era item (the hub view is the first consumer that reads this log across multiple spaces and would most benefit from tamper-evidence, not just tamper-resistance). | **OPEN** — INFO carry, no committed target rung yet (loosely associated with v3.x, not binding on it). | `docs/internal/security/security-audit-v2.19.0.md:36,53` |
| `v2.19.5-CODEOWNERS-1` | No enforced review gate exists over PRs that touch `cowork.lock.json` (or any other CODEOWNERS-listed supply-chain file), and v2.19.5 restores a third-party content-ingestion path (`sync-agency.yml`) that had been dormant (frozen pin) since 2026-07-01. Four enforcement facts, verified live against this repo (ADR-028 v2.19.5 amendment §(d)): (1) no step in `sync-agency.yml` gates on a content-scan `requires_review` hit — it is labeled and warned about, never blocked; (2) the only post-PR-creation `exit 1` (the SPDX-drift step) is structurally unreachable, since the accumulator hardcodes `--arg spdx "MIT"` against 110/110 currently-`"MIT"` entries; (3) **no CI runs on a sync PR at all** — verified against the two real historical sync PRs, `#27`/`#31` (`statusCheckRollup=0` on both, `required_status_checks` not enabled) — so every `quality.yml` job, including the new `sync-verify-ratchet`, is a post-merge detector on this path, never a gate; (4) branch protection requires **zero** approvals and **zero** passing checks (`approvals=0`, `codeowners=false`, `rulesets=0`). **Fifth fact, now partially closed by this cycle:** all six supply-chain `.github/CODEOWNERS` rows named `@msitarzewski` — the **upstream** repo's owner, not a collaborator here — and were therefore **inert**, not merely unenforced, compounding facts 1-4 with a second, independent absence. v2.19.5 re-points those six rows to `@jmlozano1990` (the sole collaborator), which makes the rows *capable* of enforcing something for the first time — but branch protection still does not require CODEOWNERS review (fact 4), so the review gate itself remains OFF. | Accepted at v2.19.5 Phase 2/3 (`@security`'s Phase-0.D CRITICAL disposition, option (b) of `AC-SYNC-CODEOWNERS-1`: correct the false claim rather than mandate an unperformable action — enabling the actual gate needs owner-level repo Settings access no pipeline agent holds). The owner explicitly accepted this exposure at the Phase 3 gate (2026-08-04), with the concrete numbers in front of them (43 net-new + 15 modified files queued for the next successful sync). Discharge routed to `docs/owner-tasks.md` OT-7, a two-step remedy (re-point CODEOWNERS rows, done this cycle; then enable branch protection, still open). | **OPEN** — binding until `AC-REQCHECK-1` is confirmed live. Status cell corrected at v2.19.15 Phase 1 (ADR-099); description and accepted-condition cells byte-unchanged. **Correction (v2.19.15, ADR-099):** the closure criterion below (`require_code_owner_reviews`) is unsatisfiable and is withdrawn — the sole collaborator (`@jmlozano1990`) is also the sole CODEOWNERS entry for every row this discharges to, and GitHub does not permit self-approval; a second, independent reason also rejects it — `actions/permissions/workflow` reports `can_approve_pull_request_reviews: true` (verified live, ADR-099 D4) while `sync-agency.yml`'s `sync-upstream` job already holds `pull-requests: write`, so even a required-approval gate could be satisfied by automation rather than a human, which would defeat rather than discharge this risk. Replaced with the criterion this cycle actually delivers: close this row only after `required_status_checks` is confirmed non-null AND `enforce_admins` is confirmed still `true`, both via a live `gh api repos/:owner/:repo/branches/main/protection` call. Original status text, retained verbatim: "**OPEN** — binding until OT-7 step 2 lands. Close this row (or move it to \"Closed this cycle\") only after `require_code_owner_reviews` is confirmed enabled via `gh api repos/:owner/:repo/branches/main/protection`." | `docs/architecture.md` ADR-028 v2.19.5 amendment §(d); ADR-099 (v2.19.15); `docs/owner-tasks.md` OT-7; `docs/internal/security/security-review-v2.19.5.md` S3/S5 |
| `AC-PUB-10` | Pre-`v2.18.0` release/CHANGELOG mismatch, 3 items (not 5 — a Round-1 miscount corrected at v2.19.6 Phase 1; `v1.3.1.1`/`v1.3.2.1` both have real entries and were wrongly also listed). `v1.0.0` (`CHANGELOG.md:1038`) and `v1.1.1` (`CHANGELOG.md:991`) have dated CHANGELOG sections but no origin tag. `v2.0.1` has an origin tag but no CHANGELOG section. All three are below the standing gate's `v2.18.0` floor, so `verify-release-surface.sh` does not (and by design should not, absent a floor change) surface them. | Recorded, not resolved, at v2.19.6 Phase 1 (`docs/spec.md` `AC-PUB-10`) — documentation only, no target cycle. Extending the gate below the floor is a named future option once the anchor-form predicate is generalized (`docs/architecture.md` ADR-077 §Maturation Path option (a), ADR-078 §Maturation Path option (b)). | **CLOSED (v2.19.8)** — see the "Closed at v2.19.8" section below for the disposition record. Status cell corrected at `plan-2026-08-27-v3-engine` Phase 1 (ADR-093); description and accepted-condition cells byte-unchanged. Original status text, retained verbatim: documentation-only carry, no target rung. | `docs/spec.md` v2.19.6 `AC-PUB-10`; `docs/architecture.md` ADR-077/ADR-078 §Maturation Path |
| `v2.19.7-LEDGER-FP` | **`scripts/verify-lock-removals.sh` (the `vendored-removal-ledger` CI job, ADR-080) is expected to run RED on the v2.19.7 PR itself, on its own introducing commit.** The job asserts `blocked_files` never silently shrinks (`AC-B5-8`) by a literal path-string diff between the previous and new `.cowork-allowlist.json` revisions. v2.19.7 repairs the pre-existing `nexus-strategy.md` `blocked_files` entry from a bare basename to its full `marketing/nexus-strategy.md` path (ADR-080 §Context (1) — the bare-basename form never actually matched `sync-agency.yml:228`'s full-path comparison, so it never fired via `blocked_files`; the file was protected only by its `blocked_patterns` entry). That path-string change is, byte-for-byte, indistinguishable from a removal to the ledger's literal-string check — it is a **false positive against `AC-B5-8`'s intent** (no protection is lost: the file remains blocked by both the new full-path `blocked_files` entry and the unchanged basename `blocked_patterns` entry) but a **true positive against the literal check as built** (the exact string `"nexus-strategy.md"` is genuinely absent from the new `blocked_files[].path` set). Verified live by @qa (Phase 5) and independently by @dev (Phase 4) — both reproduced the RED and confirmed the false-positive read is correct. | Accepted at v2.19.7 Phase 5 (`@qa`), on `@security`'s S11 grounds (already recorded in ADR-080 §Maturation Path): do not add a REPAIRED classification dimension to a removal-detection control introduced in this same cycle — doing so raises the odds the new control itself is wrong, which is the exact risk this cycle exists to reduce. No new control logic was written; this row is the accepted-risk record instead. A REPAIRED classification (symmetric with the existing MOVED classification, `AC-B5-9`) is named as a follow-up candidate in ADR-080 §Maturation Path, to be picked up alongside that section's existing S11 trigger. | **SELF-RESOLVING AT MERGE** — this row documents a one-time, already-understood RED on this PR's own `vendored-removal-ledger` run; it is not a forward-carried risk. Once this PR merges, `main`'s `blocked_files` already contains the repaired full path, so no future PR's base→head diff reproduces this specific false positive (a *different* future rename would need its own accepted-risk record, not a re-use of this one). Retained here as the record, not moved to "Closed this cycle," because the RED itself has not yet been observed by CI as of this row being written (Phase 4/5, pre-merge). | `docs/architecture.md` ADR-080 §Amendment/§Maturation Path; `.cowork-allowlist.json` diff (this PR); QA-1 (Phase 5 finding) |
| `v2.19.9-SKILLSTUDIO-TARGET` | **`skill-studio`'s step 8 keeps writing a proactive-surfacing block into the workspace's `CLAUDE.md`, and the write's benefit premise is now falsified while its cost is unchanged.** (1) **Condition accepted:** the write is retained because there is no third target — `project-instructions.txt` is inert until the user manually re-pastes it, and no end-user workspace has a literal `global-instructions.md` — **not** because the write is safe, and **not** because the file is reliably loaded. (2) **What changed:** ADR-082 falsifies the "auto-loaded every session" premise the write's benefit rested on; ADR-083 corrects the basis. The **benefit** (a reliably-read proactive surface) is falsified while the **cost** (persistent, model-authored instruction text with a proven marker-breakout history, QA-1/v2.12.0) is unchanged — the benefit/cost ratio inverted, and the write is retained knowingly on the strength of "no third target," not on a safety or reliability claim. (3) **What did NOT change:** the mandatory Phase-2 `@security` hard gate on any edit to `skill-studio/SKILL.md` stays MANDATORY, unconditionally, over-determined on three independent grounds (ADR-083 D3) — `hld.md:35` principle 4's own text ("regardless of blast radius"), the older and separate **ADR-044** authority, and QA-1's demonstrated, premise-independent marker-breakout catch. (4) **Honest limitation, named as such:** the surfacing write is the **only inspection-class link in the step-8 chain** — sub-steps 8.1 (slug charset gate), 8.2 (kit-checkout guard), and 8.5 (literal-string write) are all executable and proven-firing (`FROZEN v2`, `docs/design-v2.19.9.md` §A.4); whether the written block is ever *read* is nondeterministic and **cannot be bound by a firing negative control** the way a guaranteed every-session load could be. (5) **Both upstream dispositions annotated:** v1.2 S4 (`docs/retro.md`, S4 row — "universal auto-load" corrected, closure was already prose-only independent of the premise, so it stays inspection-class) and v2.12.0's mandatory Phase-2 gate justification (`docs/retro.md`, `[v2.12.0]` entry — right outcome, partly wrong stated reason, now resting on the standing grounds above). (6) **Closing condition:** this row closes when **either** a genuinely auto-applying, UI-entered instruction target exists and step 8 writes there instead (ADR-083 §Maturation Path future-state option (a)), **or** the live Cowork paste transcript / "the one cheap test still unrun" (carried to v2.19.10) returns a determinate answer about workspace-file read reliability that lets the trigger be bound by a firing negative control. | Accepted at v2.19.9 Phase 1 (ADR-083 D1/D3) and re-confirmed at Phase 2.D (`@architect`/`@qa` both PROCEED-WITH-CONDITIONS) — the write is knowingly retained on "no third target," never on a safety or load-reliability claim. | **OPEN** — binding until the closing condition in element (6) is met; no target cycle (gated on a platform capability or an evidence-gathering item carried to v2.19.10, neither owned by this repo's own roadmap). | `docs/architecture.md` ADR-082/ADR-083; `docs/design-v2.19.9.md` §A.4 (`FROZEN v2`); `docs/retro.md` v1.2 S4 row + `[v2.12.0]` entry (both dated-corrected this cycle) |
| `CF-v2.19.6-A` | **The ADR-076 D1 pre-create design is incompatible with `softprops/action-gh-release`'s update path, and defeats itself on real execution.** The action takes its **create** path when no Release exists (how `v2.19.0`–`v2.19.3` all succeeded, 2 assets each) and its **update** path when one does. Pre-creating the Release — which is the entire point of ADR-076 D1 — is precisely what forces the update path, which returns **403 `Resource not accessible by integration`**. Proven by direct comparison of two production run logs, not inferred: run `30253778595` (`v2.19.3`) logged `👩‍🏭 Creating new GitHub release for tag v2.19.3...` → success, 2 assets; run `31178636769` (`v2.19.4`) logged `Found release v2.19.4 (with id=366735757)` → `updateRelease` → 403. **Ruled out by checking rather than assuming:** workflow `permissions: contents: write` is byte-identical at `5fee6f9` and `main`, and the four earlier successful runs prove the token can write — this is an **operation-level** refusal, not an under-declared permission. **Explicitly NOT proven:** that repo `default_workflow_permissions` is `read`. Recorded as a lead for v2.19.7 to test, never as the answer — do not cite it as the cause. **Public state left behind:** `v2.19.4` has tag + Release + correct body + **0 assets** (strictly better than the pre-cycle "no tag, no Release"; precedented by `v2.12.0`, which has sat at 0 assets since a prior failed run). `v2.19.5`/`v2.19.6` unpublished; `/releases/latest` still resolves to `v2.19.3`. | Accepted by explicit owner decision 2026-08-07 (option 1 of 3: "diagnose properly, route the fix to v2.19.7, leave `v2.19.4` as-is"), taken with the proven run-log comparison in front of them. Scope A halted at publish 1 of 3 per its own pre-declared stop condition — publishes 2 and 3 were never attempted, and the tag was **not** deleted (the script's own printed step-2 remedy, "delete and re-push the tag", is the WRONG remedy here: `AC-PUB-13`/@security's S7 said check `gh run list` before any deletion and never delete a tag with a live Release attached on a slow poll alone — guidance written for exactly this moment, which held and prevented destroying a live public Release for the wrong reason). **Recommended v2.19.7 fix: have `publish-release.sh` upload the assets itself** — it already holds a token that can update and already builds the archives' provenance, and doing so **removes the workflow dependency entirely**, retiring the ADR-076 D3 question rather than continuing to depend on it. Alternatives rejected: flipping the repo default workflow permission is an owner Settings action that broadens permissions repo-wide to fix one job; changing the action or its invocation keeps a two-step design whose two steps disagree about who owns the Release. | **CLOSED (v2.19.8)** — see the "Closed at v2.19.8" section below; superseded by v2.19.7's asset-upload fix, live-verified (2 assets). The close condition stated in the original text was MET. Status cell corrected at `plan-2026-08-27-v3-engine` Phase 1 (ADR-093); description and accepted-condition cells byte-unchanged. Original status text, retained verbatim: blocking `AC-PUB-3` and `AC-PUB-15`; `AC-PUB-2` partially met (tag + Release + body, no assets); `AC-PUB-1` State C not reached. Binding on v2.19.7's `/spec`. Close only after a real `publish-release.sh` run produces a Release with 2 assets attached. | `.claude/projects/claude-cowork-config/scratchpad.md` (Scope A halt, 2026-08-07T12:45:00Z); `docs/architecture.md` ADR-076 D1/D3; run logs `30253778595` / `31178636769` |
| `v2.19.11-PULL-ROW-1` | **`skills/pull-updates/SKILL.md` specifies refusal for a malformed *manifest* but has no equivalent clause for a malformed registry *row*, and the poisoned-backfill defense depends on reading a `sha256` out of exactly such a row.** Three facts, verified live against the file at `b7b8447`: (1) `:30` carries an explicit REFUSE clause, and it is scoped to `cowork.install.json` — *"unparseable, truncated, or schema-invalid … REFUSE to offer or apply any update"*; (2) `:49`, `:74` and `:83` all require each backfilled safety skill's bytes to be *"byte-verified against that slug's `curated-skills-registry.md` `sha256` entry BEFORE going live"*; (3) no clause anywhere covers the case where that row is structurally damaged. On a pipe-shifted row, field 8 holds `mandatory-infrastructure` rather than a hash, and "that slug's `sha256` entry" is not well-defined: the runtime prose-gate may refuse, may compare against the wrong field, or may treat verification as unavailable and skip it. **Undefined is not fail-closed.** Pre-existing — not introduced by v2.19.11. | Accepted at v2.19.11 Phase 2 (@security S5, carried from 0.D R1 as a named, deliberately-unfixed known problem; owner decision at the Phase 3 gate). Two grounds. **(a) An independent control now covers the reachable path:** v2.19.11's AC-8b/AC-9b standing per-row CI gate makes a structurally damaged `self-apply` or `prompt-gate` row unmergeable into the repository the registry ships from — verified RED by @security in six damage directions (pipe injection into either row, row deletion, duplicate row, uppercase hex, missing file) and GREEN on both a benign whitespace reflow and this cycle's real rewrite, plus a self-test leg that converts a sabotaged checker's unearned GREEN into RED. **That gate is load-bearing for this acceptance** — without it the undefined state is reachable through an ordinary merge; with it, only a user hand-editing their own workspace copy reaches it. **(b) Fixing it here costs more than it buys:** `pull-updates` is itself a hash-gated registry row, so editing its `SKILL.md` forces a **third** gated-row `sha256` bump inside a cycle whose highest-risk item is already "we are editing gated rows." | **CLOSED (v2.19.13)** — closed by `tests/pull-updates-firing-controls.md`'s recorded invocation (AC-S5b), never by AC-S5a's clause alone and never by the AC-8b/AC-9b CI gate alone (that gate covers the repository the registry ships from, not a user's already-edited workspace copy — this row's own prior text). `skills/pull-updates/SKILL.md` now carries the malformed-registry-row refusal clause ("Backfilling the three mandatory safety skills", rule 3, symmetric with its `:30` manifest clause). A real invocation (Claude Sonnet 5, model ID `claude-sonnet-5`, 2026-08-27) against a damaged `self-archive` row (pipe-shifted, field 8 = `mandatory-infrastructure`) produced the specified refusal verbatim; the same check left the real, undamaged row untouched (GREEN control). **Two residuals, both named, neither closed by this invocation:** **model drift** — a future model version may phrase the refusal differently while still refusing in substance; the sha256 gate on `skills/pull-updates/SKILL.md` makes the *prose* durable, not a future model's *behavior* against it — and **per-slug variance** — this invocation is AC-S5b's own stated 1-of-3 proxy, exercising `self-archive` only; `self-apply` and `self-upgrade` share the same clause but were not separately invoked this cycle. If AC-8b/AC-9b is ever removed, relaxed, or has its `NF`/hex shape widened, this row's acceptance basis is void and must be re-opened and re-decided. | `docs/internal/security/security-review-v2.19.11.md` S5; `docs/design-v2.19.11.md` §L; `skills/pull-updates/SKILL.md` "Backfilling the three mandatory safety skills"; `tests/pull-updates-firing-controls.md` |
| `CF-v2.19.13-DECISION3-RESIDUE` | **3 remaining `§Decision (3)` mis-pointer loci — citations that use `§Decision (3)` to denote ADR-088's Class A/B reference-freeze ruling, whose correct target under the body-authoritative numbering (`docs/architecture.md`'s v2.19.13 ADR-088 amendment, §3a) is `(2)`.** Independently re-verified this session, applying the amendment's own stated inclusion test to each hit: `docs/internal/security/security-audit-v2.19.11.md` (2 occurrences — the `S12` finding row, and the paragraph beginning *"ADR-088 §Decision (3) rules these Class B and frozen"*) and `docs/retro.md` (1 occurrence — the v2.19.11 retro entry's WARNING-carry paragraph, *"ADR-088 §Decision (3) and ADR-090 give opposite answers for the same three files"*). A second `retro.md` hit and a `design-v2.19.11.md` hit for the literal string `§Decision (3)` were checked and correctly excluded — both cite ADR-089's own Decision (3), not ADR-088's. | Deferred by decision at v2.19.13 Phase 4 (`docs/spec.md` B0 item 3; `docs/architecture.md`'s v2.19.13 ADR-088 amendment record, §3). A full population census beyond these 3 named loci was judged too heavy for a patch cycle. All three sit in append-only historical records and are `export-ignore`d maintainer surface, so no in-place edit is available even were the cycle larger — the discharge is a named, reachable deferral (this row), per the amendment's own reachability rule (item 1, clause (ii)). | **OPEN** — named, not scheduled. This row IS the reachability discharge the ADR-088 amendment's §3 points to — no local note is owed at any of the 3 occurrences because each sits in `export-ignore`d maintainer surface and this row states why. Retirement path: `docs/architecture.md` ADR-092 §Maturation Path option (b) (a generated citation-site enumeration would dissolve population-boundary gaps of this shape generally, not only for this one census). | `docs/architecture.md` ADR-088 amendment record (v2.19.13, §Decision (2)/(3)); `docs/design-v2.19.13.md` §F; `docs/internal/security/security-audit-v2.19.11.md`; `docs/retro.md` |
| `plan-v3-engine-REGISTER-AGGREGATION` | **The carry-forward register aggregates open, unowned items sourced from the export-ignored report families, and aggregation is a distinct exposure from the sources it draws on.** Scattered findings across many reports and one ranked list of what is still open and unowned are not the same artifact, even when every constituent fact is identical. Two mitigations were applied at `plan-2026-08-27-v3-engine` Phase 2 (finding S1): the register lives at `docs/internal/carry-forwards.md`, inheriting the **directory-prefix** `export-ignore` (re-verified by `git archive HEAD` — **419 entries, 0 under `docs/internal/`, against 19 shipping top-level `docs/*.md`**; negative-controlled, since **84 tracked files exist under `docs/internal/`**, so the zero is a real exclusion and not an empty directory); and its security/QA section carries **origin-document pointers instead of reproduced finding detail**. **Verify placement with `git archive`, never `git check-attr`** — the latter returns `unspecified` under a directory-prefix rule and reads as a false all-clear. | **Accepted as a residual WARNING at Phase 6 (`@security`), knowingly carried, NOT closed.** The grounds are precise and should not be paraphrased upward into a closure: **this repository is public** (`gh repo view` → `"visibility":"PUBLIC"`), and every constituent finding was already readable in a public clone before this cycle. `export-ignore` withholds a file from the **release archive**, not from `git clone` or the web UI. **The remedy therefore leaves an increment in convenience, not in disclosure — no fact is newly public.** That is exactly why this is a WARNING and not a CRITICAL, and equally why it is not closed: the aggregation advantage to a reader who already has the clone is reduced, not eliminated. | **OPEN** — accepted, carried; no target rung. **Owner: repository owner** (every closing condition needs a decision no pipeline agent can take). **Closes only on one of three, all outside a documentation cycle's reach:** (a) the repository becomes private; (b) `docs/internal/` ceases to be tracked in git, so the content is not in any clone; or (c) the register's security/QA section is removed outright, which forfeits the visibility ADR-093 exists to create. **Relocation and pointer-substitution do NOT close this row** — they are the mitigations already applied and counted. | `docs/internal/security/security-review.md` S1 (Phase 2, CRITICAL as raised); `docs/internal/security/security-audit.md` (Phase 6 disposition — residual WARNING, accepted); `docs/architecture.md` ADR-093 §Decision (1) placement note |
| `ADR-080-TRIGGER-D-2026-09` | **ADR-080's own §Concrete revisit triggers item (d) — "the first sync in which a basename-changing rename is observed... arms no later than the 2026-09-01 cron" — HAS FIRED.** PR #125 carries two basename-changing renames: `engineering/engineering-threat-detection-engineer.md` → `security/security-threat-detection-engineer.md` (byte-identical, `content_sha256` `83bfaabe485d51965ba6474de3eb6e00f67fd7f76e5b34ca1d3fb385a8fa3d97` unchanged, independently re-verified 2026-08-31) and `engineering/engineering-security-engineer.md` → `security/security-architect.md` (content changed, `d6ce02769f3dbe732badc886e34531c8eb9393aa19b170651e256d482232c629` → `b1a68e9614f7adb43938f5bd9964f6e41250febc9a57f691eefcbab58d5b1df1`, both independently re-verified 2026-08-31). Trigger (d)'s own text names S11 content-hash blocking as the thing that becomes due. | **Re-deferred at v2.19.16 Phase 3/4 by the repo owner, not lapsed silently.** D2(b) was selected over D2(c) (which would have implemented S11-shaped compare-API rename classification) specifically because a Tier A edit to the guard that exists to stop content leaving silently, done in the same cycle that introduces the guard's own generalisation (`AC-B5-4`, D4(b)), raises the odds the guard itself is wrong — the same reasoning ADR-080 §Maturation Path already gives for declining S11 the first time. Two `.cowork-allowlist.json` `blocked_files[]` entries (`permanent: false`) name the deferral explicitly and each carries its own un-block trigger in its `reason` field, naming both entries together, so a future reader does not need this row to find them. | **OPEN — re-deferred, not closed.** New trigger, replacing the lapsed 2026-09-01 date rather than silently re-arming the same one: **(i)** the `security/` onboarding cycle named at `CF-v2.19.16-SECURITY-ONBOARD` (`docs/architecture.md` ADR-100 §F) actually starts, at which point S11 should be built alongside it rather than separately; or **(ii)** a THIRD basename-changing rename is observed in any future sync, at which point declining S11 a second time needs its own, harder-to-repeat justification. | `docs/architecture.md` ADR-080 §Concrete revisit triggers (d); ADR-100 D4 (D2(b) selection); `docs/internal/security/security-review-v2.19.16.md` S3; `.cowork-allowlist.json` `blocked_files[]` (2 new entries, v2.19.16) |

## Closed this cycle (for context — not carried as open rows above)

- `CF-v2.19-A` (self-apply Quality-criteria prose staleness) and `CF-v2.19-B` (semver-compare.sh malformed-input exit-code contract) were both labeled "next-patch non-blocking" INFO carries at v2.19.0 Phase 6 (`docs/internal/security/security-audit-v2.19.0.md:51-52`) and were folded into and closed by this cycle (v2.19.2, AC-DIST-8/AC-DIST-9). They are not open items and do not appear as rows above.
- `CF-v2.19.5-F` — closed at v2.19.6 Phase 1/4 as **MISDIAGNOSED, not fixed** (`AC-PUB-8`). The
  original row claimed all five pre-existing releases (`v2.18.0`–`v2.19.3`) "fail permanently
  with no remedy path" because the post-condition rejected any body without the dotted version
  string. Re-diagnosed live 2026-08-07 by reading all five bodies through `gh release view`:
  every one of them **does** name its version, in the house anchor form
  (`CHANGELOG.md#<dots-stripped>---`) GitHub's slugifier produces from this repo's own
  `## [x.y.z] - date` headers — the bodies were never broken. `v2.19.1`'s single dotted-form hit
  was an accidental pass, traced to an unrelated editorial-provenance sentence added at
  v2.19.2, not evidence the old predicate worked. The other four were never broken either. The
  prescribed fix — widen the `gh release edit` repair branch to fire on "missing version
  string" — would have overwritten four curated, irreplaceable editorial bodies (as little as
  652 bytes each) with raw multi-kilobyte CHANGELOG excerpts, destroying content to repair
  nothing. **Actual fix:** the predicate itself was too narrow (dotted-only); widened to accept
  the anchor form too, in one shared definition (`scripts/release-predicate.sh`) — see
  `docs/architecture.md` ADR-077 §D1/§D2.

## Closed at v2.19.8 (additive closures — the OPEN rows above are left byte-unchanged; this section is the ledger-truth-repair record, per `docs/spec.md` Scope B)

- **`CF-v2.19.6-A` — CLOSED.** The row above (`403 Resource not accessible by integration` on the
  update path) is superseded: `v2.19.7`'s recommended fix — `publish-release.sh` uploading
  assets itself, retiring the `release-assets.yml` upload dependency entirely — shipped and is
  live-verified. `gh release view v2.19.7 --json assets` returns exactly 2 assets
  (`cowork-starter-kit-2.19.7.tar.gz`, `cowork-starter-kit-2.19.7.zip`), re-run this session.
- **`v2.19.7-LEDGER-FP` — CLOSED.** The row above documents the self-predicted RED on the
  `vendored-removal-ledger` job, on v2.19.7's own introducing commit. Confirmed via
  `gh api repos/jmlozano1990/cowork-starter-kit/actions/jobs/93105955137` (part of workflow run
  `31258671814`, branch `release/v2.19.7-finish-the-storefront`): job `Vendored Removal Ledger
  (ADR-080)`, `conclusion: failure` — the predicted RED, reproduced. Its GREEN counterpart, once
  the repaired `.cowork-allowlist.json` was on `main`, is confirmed via
  `gh api repos/jmlozano1990/cowork-starter-kit/actions/jobs/93117624498` (workflow run
  `31263420714`, branch `qa/v2.19.7-retro`): same job name, `conclusion: success`. A second
  independent GREEN confirmation exists at job `93124881727` (run `31266301310`, branch
  `docs/v2.19.7-upstream-pr-urls`), also `conclusion: success`. **Note on identifier spaces
  (binding per `docs/spec.md` Scope B item 6):** the RED workflow-run ID `31258671814` and the
  three job IDs (`93105955137` RED, `93117624498`/`93124881727` GREEN) were re-resolved live this
  session via `gh api .../actions/jobs/<id>`, not copied forward on trust; the previously-recorded
  workflow-run ID pair (`31264226626`/`31264226226`) 404s against the live API — neither
  resolves — and is superseded here by the job-ID pair that does.
- **`AC-PUB-10` — CLOSED (disposition recorded, v2.19.8).** The row above's two CHANGELOG
  citations (`CHANGELOG.md:1038` for `v1.0.0`, `CHANGELOG.md:991` for `v1.1.1`) were stale;
  re-verified this session at content-anchored positions, citations that resolve on re-run rather
  than a number that will drift: `grep -n '^## \[1\.0\.0\]' CHANGELOG.md`;
  `grep -n '^## \[1\.1\.1\]' CHANGELOG.md`. `v2.0.1`'s missing CHANGELOG
  section is backfilled this cycle (`AC-C1`). The full disposition — documented-unpublished, no
  retroactive tag or Release for either `v1.0.0` or `v1.1.1` — is `docs/design-v2.19.8.md` §B and
  `docs/spec.md` `AC-C2`; the CHANGELOG's own `## Release surface` preamble (added this cycle)
  carries the reader-facing record.

## Maintenance

Add a row here when a future Phase 2/Phase 6 security review accepts a risk under an explicit condition rather than fully resolving it. Remove (or move to a "Closed" note, as above) a row once the accepting cycle's condition is fully discharged — cite the closing cycle/doc when doing so.
