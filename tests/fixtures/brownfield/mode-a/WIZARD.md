# [FIXTURE STAND-IN] Cowork Setup Wizard

This file exists ONLY to trip the kit's own boolean detection signal —
`WIZARD.md present in the workspace root` (`WIZARD.md:306` in the real kit) — for
`tests/collision-safety-firing-controls.md`'s **mode-a** fixture. It is a synthetic
placeholder, not a working copy of the real interview script, and is never executed.

Real kit source of truth: the repository's own top-level `WIZARD.md`.
