#!/usr/bin/env bash
# [SYNTHETIC FIXTURE] tests/fixtures/brownfield/mode-a/scripts/ — not the kit's own
# scripts/. Exists only to give WIZARD.md:308's 7b move list a same-named, non-kit
# `scripts/` directory to collide with, per design-v2.19.18.md §F OQ-1.
echo "placeholder build step for the fixture's own (fictitious) project"
